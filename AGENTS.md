# Level0 主线协作规则

适用于整个仓库。用户当前指令优先。首先只读本文件和 [CURRENT_STATE.md](CURRENT_STATE.md)，按当前任务再读相关源码；不要批量加载历史报告、提示词目录或整个聊天记录。

## 唯二长期目标

1. 世界事实、身份、经历、关系、产权和承诺独立于模型；更好的模型经过验证后能继续提升同一个世界。
2. 建成 SAO 式、由 AI 居民生活与选择持续推动的可进入世界。当前为艾恩葛朗特第一层，代号 Level0。

当前近期目标是可招募合作者的原型：好看的小镇、5–10 个自主生活居民、可观察的真实事件、玩家干预后的不同选择、可下载体验及自带模型入口。100 名参与者和 1 万 GitHub star 是社区愿景，不是工程验收承诺。

## 开发与 token 纪律

- 以可展示、可复现的行为成果管理进度。每批只选一个可见结果，先复用已有功能和资产，避免泛化重写与重复建模。
- 普通脚本完成检索、计数、轮询、运行监控和格式整理。无变化时不反复唤醒高级模型。长测试由有截止、可保存状态的程序运行。
- 主代理仅承担方向取舍、必要集成、Blender 样板/风格与实机验收。有独立且必要的工作才分发子 agent；通常 1 名，最多 2 名并行工人，不为占满并发而派活。
- 子任务只给目标、相关文件、可编辑范围、接口、验收与停止条件；默认不复制完整历史，不层层派发。沿用长任务前先判断是否已超出原范围。
- 上下文建议目标 1–3 万 token；超过约 6 万先考虑精简或结束后以短交接续接。这是工作策略，不声称已配置硬性上限。不得丢弃必要的正确性约束来凑数字。
- 新增约 2,000 万原始开发 token 是首阶段复盘点：必须交付可直接观看、可复现的真实居民事件；未达到则分析阻断并调整范围，不自动开启下一晚。该检查点目前需统计执行，不是已实现的自动熔断。
- 按任务和时间窗口累计用量增量；分别记录输入、其中缓存输入、输出及实际费用。缓存是输入子集，推理是输出子集，不能重复加；不能相加每行累计计数。Codex、Kimi、其他任务和未知来源分别记账。
- 旧 1,000万–4,000万全量 MVP 估算已撤回。1亿–3亿/1–2周只作为招募原型的低置信度规划假设，不是花费授权、完工承诺或订阅额度换算。
- 检查按风险选择：交易/存档/资源守恒要验证；低风险文档改动不重跑整套引擎。不因时间尚早重复通过的检查。

## 分工

| 工作 | 默认执行者 | 升级条件 |
| --- | --- | --- |
| 统计、文件整理、无变化监控 | 脚本 | 确有需要判断的异常 |
| 明确范围的工程、UV、导出和整理 | 可用的轻量模型，当前 Luna | 有具体复现的复杂状态或集成问题时转 Sol |
| 跨模块一致性与复杂故障 | Sol | 需要重新决定架构或方向时转 Astra |
| 产品取舍、原创 Blender 样板、轮廓风格、实际画面终审 | Astra | 按任务需要使用，不包揽机械工作 |
| NPC 的个人观察、目标、交流与自主选择 | Kimi/经验证的替换模型 | 不用开发代理编写指定剧情冒充自主选择 |

5.3/Spark 只有当前工具确实可调用并验证成功才使用；上轮未成功接入，不重复失败尝试，不声称已用到独立额度。具体派工模板见 [Agent_Workflow.md](ThreeHearthsVillage/Docs/Prompts/Agent_Workflow.md)。

## 世界与运行边界

- 同一世界长期延续，不因重启、换模型、演示或排错随意重建。角色信息来自本人观察、经历或明确收到的信息；意图、批准、安装、实际使用分开记录。
- 金钱、材料、食物、位置、工时、产权与操作去重由持久状态核验。真实 Kimi 行为、人工测试、回放和本地回退分别标注，不强迫居民产生预设故事。
- Godot 是当前场景方向；目前 UE 仍是社会状态唯一权威写者，Godot 生活窗只读。先复用此基础做一条完整体验，不并行扩展两套社会、不冒称迁移完成。
- 主代理统一拥有共享世界写入、引擎、Blender 与共享构建；子 agent 不另开付费运行、引擎或修改正式存档。只清理自己拥有的进程。
- Kimi 继续累计100元授权/95元分配上限，保留历史未决和外部负债，预算不是消耗目标。普通收费思考至少30分钟现实时间，值勤白日梦6小时；真实事件按既有策略触发，倍速不放大收费频率。
- 旧夜间截止、PID、心跳及 reset 不是当前启动指令。到期任务保持结束；后续运行按用户当前授权和新的明确截止执行。跨电脑不从空账本重新获得100元额度。
- Git 只传项目代码、资源和精选脱敏证据；密钥、运行鉴权、费用数据库、Saved、缓存、崩溃转储及构建产物不上传。保留原始失败，不把中断记成成功。
- 旧国王征税、旧时间穿梭路线与历次过期提示词均为历史，不能覆盖本主线。运行时 NPC 提示词只在相关源码维护，不向其中加入开发派工、预算估算或宣传目标。
