# 《零时回响》当前功能总览

本文记录当前仓库中已经可以运行的游戏功能、场景编辑功能、Codex 美术资产工作流以及主要技术约定。产品方向与后续开发顺序见 [`PROJECT_ROADMAP.md`](PROJECT_ROADMAP.md)，Codex 处理单个美术任务的规则见 [`docs/EDITOR_ASSET_WORKFLOW.md`](docs/EDITOR_ASSET_WORKFLOW.md)。

## 1. 运行方式

项目是浏览器内运行的 Three.js 2D 横向场景原型。

```bash
npm install
npm run dev -- --port 3004 --strictPort
```

游戏地址：

```text
http://127.0.0.1:3004/
```

直接进入编辑器：

```text
http://127.0.0.1:3004/?editor-preview
```

生产构建：

```bash
npm run build
```

## 2. 游戏模式

### 2.1 玩家移动与镜头

- 固定二维正交视角，玩家、场景、Boss 与投射物都由 Three.js 渲染。
- `A / D` 左右移动。
- `W / Space` 跳跃。
- `S / Ctrl` 下蹲；位于单向平台时可以向下穿过当前平台。
- 玩家具有重力、地面、平台和场景组件碰撞。
- 玩家在所有场景资产之前渲染，透明 PNG 和编辑器组件不会遮住角色。
- 武器子弹和回旋扳手位于场景资产之前、玩家之后，保证战斗反馈清楚。
- 打开或关闭其他面板不会清空正在按住的移动键；按 `Q` 切换时代不会让角色短暂停顿。

### 2.2 时间切换

- `Q` 在固定的 `2047` 与 `2147` 两个时代之间切换。
- 切换过程不刷新页面，通过统一时代混合值连续过渡。
- 场景颜色、建筑状态、灯光、粒子、动画与独立 PNG 资产跟随时代混合。
- 玩家和时间锚背包不随时代切换重置。
- 过去发生的关键事件会改变现代的场景状态。
- `R` 重置当前时间线和任务进度。

### 2.3 交互、背包与任务物品

- `E` 拆卸、拾取、安装或使用附近的场景装置。
- `B` 打开或关闭 20 格时间锚背包和 10 格快捷栏。
- 03 号机械手轮可以在 2047 年从闸门拆下，随时间锚背包带到 2147 年并重新安装。
- 时锁爆破器与维修铣刀用于实验室防爆门的跨时代任务链。
- 任务物品使用后会从背包格子中移除。
- 场景 HUD 会显示目标、血量、时代、历史事件进度、快捷栏和交互提示。

### 2.4 武器与攻击

快捷栏 `1 / 2 / 3 / 4` 对应四种不消耗弹药的武器：

| 按键 | 武器 | 行为 |
| --- | --- | --- |
| `1` | 小型时相校准器 | 鼠标自由瞄准的直线脉冲子弹 |
| `2` | 双手脉冲钻 | 按住连续近战钻击 |
| `3` | 液压挥击锤 | 单次前向挥砸 |
| `4` | 回弹开口扳手 | 投出、追踪、命中或超时后返回 |

- `J / 鼠标左键` 使用当前武器。
- 鼠标控制 360 度瞄准，角色会跟随目标左右转身。
- 近战武器使用线段范围检测，远程武器使用每帧运动线段检测，快速子弹不会轻易穿过物理组件。
- 普通子弹命中物体后消失；回旋扳手命中后转入返回状态。
- 武器在攻击和短暂准备时间内显示在手中，待机时回到背部或腰部。

### 2.5 场景物理组件与碎裂

Editor 中标记为“有物理、可破坏”的组件可以参与游戏碰撞和受击逻辑。

- 支持玩家可推动的物体。
- 支持带碰撞的静态物体。
- 支持没有碰撞的纯视觉物体。
- 玩家武器命中物理组件时，原始贴图会被直接切成多个保留 UV 的透明碎块。
- 默认分成 4 块，可在高级设置中调整为 4 至 8 块。
- 每块都是简单凸包三角形，由 CPU 计算速度、重力、旋转、地面反弹、阻尼和休眠。
- 碎裂没有额外爆炸、闪光或粒子特效，只表现贴图撕开和碎块散落。
- 碎裂后的原组件停止参与玩家碰撞；进入 Editor 时恢复完整状态，方便继续编辑。
- 两个时代切换时，碎块仍使用同一组件的 2047/2147 贴图进行交叉混合。

### 2.6 关卡与 Boss

- 上层矿场包含破碎机、传送带、通风维修站、03 号闸门、深井、轨道和矿车。
- 2047 年升降机可以往返地面与地下实验室，2147 年对应区域表现坠毁和坍塌。
- 实验室任务链包含档案调查、身份核验、门体终检、暗槽加工、爆破器安装和现代远程触发。
- Boss“03 型时晶采掘机·掘脉者”具有生命值、阶段切换、核心受击和死亡状态。
- 一阶段包含长激光锁定与冲刺；二阶段包含短激光弹幕、更快冲刺和跳钻。
- Boss 房包含三级单向维护平台，支持跳跃、下蹲和穿台躲避。
- `?boss-preview`、`?phase2-preview`、`?dash-preview`、`?jump-preview`、`?laser-preview` 可用于开发预览。

## 3. AI 场景资产编辑器

### 3.1 设计原则

Editor 面向不会美术和技术的使用者，默认只展示最常用的操作：

- 移动物体
- 圈选已有
- 添加新物体
- 画风参考
- 保存并试玩

精确坐标、碰撞尺寸、旋转速度、透明度等参数放在“设置”或“更多调整”中，不影响普通用户完成主要流程。

### 3.2 添加新物体

1. 点击网页右侧的 `EDITOR`，游戏暂停。
2. 点击“＋ 添加新物体”。
3. 在场景中按住鼠标拖出目标区域。
4. 选框旁会直接出现“这个框里想添加什么？”输入框。
5. 输入自然语言，例如“这里放一台会缓慢旋转的旧工业风扇”。
6. 点击“复制给 Codex”。
7. 到 Codex 中粘贴并发送自动生成的提示词。

系统会自动把以下信息写入任务和剪切板提示词：

- 用户输入的自然语言描述
- 框选世界坐标、宽度、高度和中心位置
- 框内现场截图
- 2047 完整状态参考图
- 2147 破败状态参考图
- 当前场景的画风、材质、调色板和细节约束
- 双时代透明 PNG 输出位置
- 完成后放回原选框的要求

自然语言会自动推断基础行为：

- 提到风扇、齿轮、旋转、转动或循环运动时，默认设为持续动画组件。
- 提到推动、碰撞、可破坏、打碎、木箱、矿车或油桶时，默认设为物理组件。
- 其他描述默认设为普通场景物。

### 3.3 选择与调整已有物体

- “移动物体”：直接点击透明 PNG 的非透明像素并拖动。
- 点击检测会读取 PNG Alpha，不会因为透明矩形画布误选空白处。
- “圈选已有”：拖框后列出框内所有独立组件和整景逻辑组件，再选择要调整的对象。
- Editor 会显示场景内所有组件边界，并用颜色区分普通、动画和物理组件。
- `Ctrl/Cmd + C` 与 `Ctrl/Cmd + V` 复制、粘贴选中物体。
- `Ctrl/Cmd + D` 快速复制。
- `Delete / Backspace` 删除选中物体。
- 可以直接修改名称、组件类别、历史版本，或让 Codex 重新生成。
- “更多调整”中保留位置、宽高、旋转、透明度、前后层级、碰撞框、碎裂块数和动画速度。
- 网格吸附与网格大小放在顶栏“设置”中。

### 3.4 组件类型

| Editor 文案 | 运行时含义 |
| --- | --- |
| 普通场景物 | 不主动运动，默认没有碰撞 |
| 会自动运动 | 当前支持围绕画布中心持续旋转 |
| 有物理、可破坏 | 可以配置推动、静态碰撞和受击碎裂 |

### 3.5 画风参考与双时代资产

- “画风参考”面板默认收起，避免遮挡场景。
- 支持上传 PNG、JPEG 和 WebP。
- 参考图分为 `2047 · 完整状态` 与 `2147 · 破败状态` 两组。
- 每个生成组件必须输出同样画布、轮廓、锚点和方向的两张透明 PNG。
- 项目采用普通高质量 2D PNG，不进行像素化、调色板量化、抖动或噪点后处理。
- 资产按 Unlit 思路制作，不在 PNG 中烘焙场景方向光、阴影或体积光。
- 2147 版本表现同一物体的老化、损坏与缺失零件，不改变物体身份。

### 3.6 版本历史

- 每个资产保存多个双时代版本。
- 可以在上一版和下一版之间切换。
- 重新生成不会覆盖旧历史。
- 页面轮询 Codex 任务结果；任务完成后自动加载新 PNG，并把新版本设为当前版本。
- 保存后版本历史和组件状态写入 Editor 状态文件。

## 4. Codex 资产任务工作流

点击“复制给 Codex”后，Vite 本地接口会创建：

```text
editor-jobs/<job-id>/request.json
editor-jobs/<job-id>/selection.png
```

`request.json` 包含：

- `jobId`、`assetId`、`versionId`
- 用户描述
- `intent`：新增或替换
- 框选截图路径
- `selectionWorldBounds`
- 组件类型与动画数据
- 两个时代的全部参考图
- 完整生成约束
- 两张输出 PNG 和 `result.json` 的指定路径

Codex 按 [`docs/EDITOR_ASSET_WORKFLOW.md`](docs/EDITOR_ASSET_WORKFLOW.md) 生成文件并写回结果。页面通过任务状态接口检测：

```json
{
  "status": "complete",
  "pastImage": "/editor/assets/...-past.png",
  "presentImage": "/editor/assets/...-present.png",
  "resultFile": "editor-jobs/.../result.json"
}
```

## 5. 本地数据接口

这些接口由 [`vite.config.js`](vite.config.js) 中的本地持久化插件提供：

| 接口 | 方法 | 功能 |
| --- | --- | --- |
| `/api/editor/state` | `GET` | 读取 Editor 状态 |
| `/api/editor/state` | `POST` | 同步保存状态到 `editor-data/` 和 `public/editor/` |
| `/api/editor/reference` | `POST` | 保存上传的画风参考图 |
| `/api/editor/job` | `POST` | 保存框选截图和任务请求，返回剪切板提示词 |
| `/api/editor/job-status?id=...` | `GET` | 读取 Codex 写回的任务状态 |

这些接口用于本地编辑流程，不是面向公网部署的鉴权服务。

## 6. 数据与目录结构

```text
index.html                         页面 UI
src/main.js                       游戏、玩家、关卡、战斗、Boss 和时间逻辑
src/editor.js                     Editor、资产版本、碰撞、碎裂和任务轮询
src/editor.css                    Editor UI
src/style.css                     游戏 HUD 和页面样式
src/vendor/three.module.js         本地 Three.js 模块
vite.config.js                    Vite 与 Editor 本地保存接口
editor-data/editor-state.json     可编辑场景状态
editor-jobs/                      本地生成任务（默认不提交任务历史）
public/editor/scene-art/          双时代整景底板
public/editor/assets/             独立透明组件及历史版本
public/editor/references/         双时代画风参考图
docs/EDITOR_ASSET_WORKFLOW.md      Codex 资产写回规范
PROJECT_ROADMAP.md                产品方向与开发路线
```

## 7. 当前技术边界

- 当前碎块物理是轻量 CPU 实现，包含地面碰撞，不包含碎块之间的完整刚体堆叠。
- 固定动画当前以持续旋转为主，尚未提供通用骨骼、序列帧或曲线编辑器。
- Editor 本地接口依赖 Vite 开发服务器；静态托管只负责展示已经生成的资产。
- 当前是垂直切片，不包含完整大地图、联机、制作系统、装备词缀或最终数值平衡。

## 8. 验证项目

当前版本已验证：

- `npm run build` 可以完成生产构建。
- `3004` 端口本地页面返回正常。
- Editor 可以进入和退出，游戏模式会暂停和恢复。
- 可以在场景中拖框、输入自然语言并复制 Codex 提示词。
- 提示词包含任务文件、框选位置、画风参考和输出回填要求。
- 组件可以独立选择、拖动、复制、删除和切换版本。
- 风扇固定旋转动画在浏览器运行。
- 玩家和武器投射物渲染顺序位于场景组件前方。
- 武器可以击碎物理组件，默认产生 4 个 CPU 碎块。

