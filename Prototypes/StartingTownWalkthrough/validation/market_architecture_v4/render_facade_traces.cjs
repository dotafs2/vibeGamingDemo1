const { chromium } = require('C:/Users/quchenxi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const fs=require('fs'),path=require('path');
const data=JSON.parse(fs.readFileSync(path.join(__dirname,'facade_traces_px.json'),'utf8'));
const out=path.join(__dirname,'facade_traces_overlay.png');
const sx=p=>p[0]/2, sy=p=>40+p[1]/2, poly=a=>a.map(p=>`${sx(p)},${sy(p)}`).join(' ');
(async()=>{console.log(JSON.stringify({helper_pid:process.pid}));const b=await chromium.launch({headless:true,channel:'msedge'});try{const p=await b.newPage({viewport:{width:1280,height:800}});let lines='';for(const o of data.objects){lines+=`<polyline points="${poly(o.contour_px)}"/><text x="${sx(o.contour_px[0])}" y="${sy(o.contour_px[0])-5}">${o.id}</text>`;for(const f of o.features||[]){const ws=f.window_outline_px|| (f.outline_px?[f.outline_px]:[]);for(const w of ws)lines+=`<polygon points="${poly(w)}"/>`;}}await p.setContent(`<style>html,body{margin:0;background:#222}#s{position:relative;width:1280px;height:800px}img{width:1280px;height:800px}svg{position:absolute;inset:0;fill:none;stroke:#ff2055;stroke-width:2;font:12px sans-serif}text{fill:#ff2055;stroke:#fff;stroke-width:.7;paint-order:stroke}</style><div id=s><img src="https://i.pinimg.com/originals/92/a3/a7/92a3a78f0b36febbef3abcb70f37ff7e.png"><svg width=1280 height=800>${lines}</svg></div>`,{waitUntil:'domcontentloaded'});await p.waitForFunction(()=>document.images[0].complete&&document.images[0].naturalWidth>0,{timeout:25000});await p.screenshot({path:out});}finally{await b.close()}})();
