# 2026-09-05 验证记录

- UE 5.8.2 插件编译通过，模型输出校验、每人独立请求名额和两项移动原生测试通过；请求名额测试使用五个居民状态，确认没有固定三并发限制。
- 本地 HTTP 并行测试完成 28 次请求，检查乱序回复、超时、预算、暂停、资源冲突和重开取消。
- 真实自主运行保存了 111 次 Kimi 请求、约 32 分钟游戏时间。三座初始房屋和六块新增农田完成；96 个决定直接采纳、15 个目标变化后采用本地备用选择。逐条 token 总量与接口合计 141764 相符。
- 该自主运行随后因应用退出而中断；退出原因未确定，最后有两项工作尚未完成，因此不能将其作为正常收尾的完整通过记录。个人原始运行历史不随项目发布。
- 隔离补验的 38 次安排覆盖全部 13 类生产建设操作，资源账目、运输、暂停、目标互斥、重开保留历史和 1144 个路线采样通过。
- 另外 30 步生产循环通过，覆盖收空重播、树木灌木再生、石矿耗尽及新资源点生产；补验不调用真实 API。
- 原创小屋修正版通过 Blender → GLB → UE 5.8.2 编辑器异步导入，导入约 3.497 秒；保留 16,788 个三角面、19 个材质和共享瓦片 UV0。已在默认村庄地图北侧主路旁落地、添加碰撞并保存；九处地面探测、村庄土地探测阻挡及 UE 实拍检查通过。对应资源与地图已同步到此项目。记录见 `Art/HearthCottage/UE_Import_Report.json`。本项不包含 PIE 中的失败回退、NPC 入住或打包运行时导入验证。

待改进：世界存档；每人持续目标及共享任务意图；低精力约束；明确向模型提供真实规则；让食物和住房对应实际人口需求。

## 2026-09-06 本机接续：PIE 导入验收通过

本机 UE 5.8.1 编译通过；12 项 Python 离线测试、5 项 UE 原生测试和 11 项真实 PIE 验收通过。已知小屋 GLB 通过隐藏编辑器工作进程导入，成功后在原位置显示，完整保留 19 个材质槽和原碰撞。两次成功导入分别耗时 6.687、6.812 秒，等待期间村庄模拟持续推进，最大采样 Slate 间隔为 47 毫秒。

验收覆盖成功后恢复、缺失/截断/无几何文件、超时、重复请求、取消、村庄重开、退出 PIE、拒绝编辑器世界及新 PIE 再导入。每个导入/清理案例结束时工作进程数为 0。修复了失效 UObject 包装的清理异常，并移除了导致命令行编辑器失败退出的废弃 `r.Mobile.VirtualTextures` 项。

原生临时世界测试有一条 `World has no context` 清理警告，测试结果通过；真实 PIE 清理另有验收。使用独立关闭 API 的配置及测试历史，未调用模型服务。当前是 Windows 编辑器预览，尚未覆盖打包运行时、任意模型或 NPC 设施接入。操作、性能边界、截图和机器可读报告见 [PIE 导入预览](PIE_Import_Preview.md)。

## 2026-09-06 模块住宅美术与施工数据

独立美术 agent 通过本机 Blender MCP 完成 38 个组件、3 个组合房型和 3 份可编辑 Blender 文件。38 件共 55,656 三角形，最高 4,860/件；几何、UV、单位法线及退化面检查通过。瓦片曲面采用平滑法线，红陶/蓝灰各 4 种色差变体，粗糙度 0.34～0.40、金属度 0。组件总览、三房型、瓦片近景和楼梯剖视均已实际渲染并看图。

离线目录支持 3 个房型 × 3 种墙材 × 2 种屋瓦，共 18 种设计。稳定计划与操作 ID、依赖顺序、搬运批次容量和材料总数守恒通过检查；非法墙材被拒绝。三份示例包含 55、72、97 次安装；所有计划明确标记尚未接入实时执行。

最终 41 份 GLB 已导入 UE 5.8.1 原生网格，导入命令行进程退出码为 0。UE 内再次核对全部源文件 SHA-256、材质槽、屋瓦参数、摆放方向及 38 件组件的米到厘米换算，全部通过。首次地图生成的 Actor Factory 路径不可用于命令行编辑器，已改为显式 StaticMeshActor；展示时发现 Rotator 位置参数造成横倒，已改用具名参数并加入直立检查。独立展示地图已保存，并在原村庄岛屿灯光下实拍。

本批美术检查使用独立离线编辑器，没有调用真实模型服务，也没有改变原村庄地图或运行中的模拟。本项不包含碰撞、NPC 寻路/上下楼、实际搬运施工、住房选择 AI、世界存档或打包运行时验收。资源用量为尚未应用的平衡草案。说明、图片与机器报告见 [模块住宅](Modular_Housing.md)。

## 2026-09-06 夜间人民币预算门禁

14项Python离线预算验收、UE5.8.1编译与6项原生测试通过。真实UE经固定本机预算代理完成3次独立Kimi决定，均被采纳；643输入、86输出、缓存0，按usage核算0.0065015元，在途预留0。此前配置测试保守占用0.01元，计入同一95元分配上限。余额查询暂未显示扣费，未因此减少账本计费。持久账本、异常处理、强制路由和完整范围见 [Kimi预算门禁](Kimi_Budget.md)。

## 2026-09-06 世界存档与执行恢复

编译及8项原生测试通过，包括新加入的材料/任务恢复、损坏文件与独占世界测试。五个建房执行阶段、预扣材料后的生产行走/工作、产出交付、生长计时、重复完成、未确认模型请求、引用校验和资源守恒均通过。

真实岛屿场景在携带木料时刷盘，强制结束专用UE进程，再启动后所有逻辑状态字段一致（自动保存版本号继续递增）。恢复后3间房正常完成；真实采集任务携带4份浆果时再次存取，完整生产状态一致，最终只入库一次，食物34，来源账仍30，木材来源账36。此轮真实API请求为0。详见[世界存档](World_Persistence.md)及[机器验收报告](Validation/World_Persistence_Acceptance.json)。旧Cropout在-nosound下的音乐蓝图空引用等模板问题已明确记录，未计作新存档功能。

## 2026-09-06 SocietyKit 城堡与工坊

32件新组件、2组合、3份Blender文件通过GLB/UV/法线/退化面和8项真实开口检查；总59,958三角，最高单件12,056。主线程已看过全部3张Blender渲染。34份GLB以本地离线命令行编辑器导入，进程退出0；源哈希、材质槽、32件米到厘米尺寸和34处直立摆放再次通过。独立展示地图使用现有岛屿灯光与Cropout房屋作对照，原村庄地图保留。离线目录区分物品搬运、单件安装和建筑方案，组合分别31和16实例；尚未声称实际经济、NPC工坊或城墙寻路已运行。详见[SocietyKit美术说明](../Art/SocietyKit/README.md)。
