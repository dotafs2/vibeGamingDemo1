# 现有素材与任务 1–4 输出一致性审计

审计时间：2026-08-10
审计基线：远端 `main@2f0b415` 与四个独立任务工作区当时的未提交输出

## 已执行的修正

1. 用户指定新提供的 1932×814 矿场设施长景替换地上段底图；`public/editor/assets/scene-*` 的 8 个独立场景拆件、可推动矿车和持续旋转风扇保持原图片、坐标、框选、动画、碰撞和交互。
2. 新增 `src/art-direction.js`，用同一世界骨架统一远景岩壁、中景设施、近景结构、过去/现代色板、接缝锚点和视差。
3. 任务 2–4 的高清位图已复制到 `public/art/`，由统一装载器按世界坐标接入；交互物和碰撞仍使用独立 Three.js 对象。
4. 新增连续巡检入口和 `tests/art-direction-contract.test.mjs`，防止后续改动破坏 2u 重叠、角色/相机比例或位图尺寸。

## 分项审计

| 来源 | 当前输出 | 一致项 | 不一致/风险 | 已执行或要求的修正 |
|---|---|---|---|---|
| 主仓库编辑器整景 | 新地上长景、风格参考、8 组拆件、矿车与风扇 | 用户指定的新底图；同一矿场题材、铁锈金属、青蓝晶体；物件可独立移动/旋转 | 地上长景为共享时代骨架，后续可补一套现代损坏材质变体 | 地上段只替换 `sceneArt`；组件图片、坐标、框选、动画、碰撞与交互保持现状；井底继续接任务 2–4 生产背景 |
| 任务 1 闸门 | 闸门机制重构、`gateMechanics.js` 与测试 | 使用现有闸门世界坐标与历史逻辑 | 当次检查没有新的独立高清闸门长卷；代码仍在其独立脏工作区 | 本分支不复制/覆盖其未提交代码；视觉使用 S01 锚点和统一骨架，后续合并机制时保留 `X=13` 接缝 |
| 任务 2 电梯 | 2047/2147 远景与透明中景各一套，1448×1086 | 高清非像素；同一竖井骨架；左上暖光和青蓝辅光明确 | 尚无运行接入；过去版整体偏亮金色；右侧与实验室存在大面积重叠 | 映射至约 `X=5.5…50.7`、井轴 `X=18.25`；置于组件后方并以原色、不降透明度显示，保留 2u 接缝 |
| 任务 3 实验室 | 两时代连续长卷及 far/mid/near 切片 | 世界合同完整：`X=18.1…64.1`、基线 `-22.4`、两端 4u+ 重叠；骨架一致 | 2047 比任务 4 更暖，切片为 RGB 而非透明，不能互相覆盖叠放 | 采用其原始纵向带切法而非重叠图层；统一 sRGB/线性过滤并保持原图色彩；S23/S34 由世界柱遮缝 |
| 任务 4 Boss 厅 | 2047/2147 大厅长卷，2172×724 | 严格正侧视；`60.75…116.25`；地面连续；机械轮廓和晶体识别强 | 两时代都偏冷；过去版与实验室暖度不完全一致；原代码透明度较低 | 采用其作者提供的 `55.5×18.5u` 映射；最终使用现代 `.72`、过去 `.76` 透明度及分时代乘色；S45 与末端柱遮缝 |

## 仍需在合并其他任务时遵守

- 任务 1 的机制代码与本分支 `src/main.js` 都有大范围改动，禁止直接覆盖文件；应以功能块或 Git 三方合并方式整合，并重新跑自动巡检。
- 任务 2 若后续改变 PNG 尺寸，必须同步修改 `src/art-direction.js` 映射和契约测试；不得由 CSS 或任意视觉缩放补偿。
- 任务 3 的历史改写交互尚未并入本美术总控分支；合并时保持 `X=44.2`，并把增长物/隔离柜放在 L4 交互层，不能烘焙进 RGB 长卷。
- 任务 4 的攻击预警和 Boss 逻辑应保持在 L5；任何新增前景机械臂不得遮住冲刺、钻击、激光警示。

## 可执行验收清单

- [x] `npm run test:art` 通过。
- [x] `npm run build` 通过。
- [x] 过去/现代自动巡检各完整循环一次。
- [x] S01–S45 在相机移动时无裸露裁边、黑缝或重复唯一物件。
- [x] 角色站立高始终约为普通门高度的 54%–72%，不随场景改变。
- [x] 地上 `Y=-4.55` 和井底 `Y=-22.4` 无断线；电梯过渡只改变相机 Y。
- [x] 左上主光方向和青蓝辅光在所有镜头一致。
- [x] 过去/现代切换只改变年代状态，不移动主结构或平台碰撞。
